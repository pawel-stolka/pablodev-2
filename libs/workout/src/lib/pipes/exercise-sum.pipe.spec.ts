import { ExerciseSumPipe } from './exercise-sum.pipe';

describe('ExerciseSumPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseSumPipe();
    expect(pipe).toBeTruthy();
  });
});
